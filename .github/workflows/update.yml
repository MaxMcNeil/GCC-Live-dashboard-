name: Update GCC Live Data

on:
  schedule:
    - cron: "0 */5 * * *"   # toutes les 5 heures
  workflow_dispatch: {}       # bouton "Run workflow" manuel
  push:
    branches: [main]

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  update-data:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Fetch data
        run: python fetch_data.py
        # Le script lui-même écrit un résumé détaillé dans l'onglet "Summary"
        # du run (tableau ✅/❌ par source + message d'erreur exact), donc pas
        # besoin de creuser les logs bruts pour trouver la cause d'un souci.

      - name: Commit updated data.json
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data.json translation_cache.json
          git diff --quiet && git diff --staged --quiet || git commit -m "auto: refresh data.json [skip ci]"
          git push
        continue-on-error: true

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: .

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
